package views

templ Index() {
	@layout(contents{body: body()})
}

templ body() {
	<h1 class="text-4xl font-bold text-center py-4">Meet the Locals</h1>
	<p>
		Letting the locals help you get a memorable trip.
	</p>
}
